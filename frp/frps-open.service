[Unit]
Description=FRP Server (Open legacy on 35735 — no auth, no TLS)
After=network.target

[Service]
Type=simple
User=frp
ExecStart=/home/frp/bin/frps -c /home/frp/frps-open.toml
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
